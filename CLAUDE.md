# 豊穣の水田（改訂版）— Claude Code 開発引き継ぎ

> このファイルは Claude Code がプロジェクトの全体像を把握するための指示書。
> ルールの大本は `hojo_suiden_rulebook.docx`。ただし **本書は友人プレイテストを反映した「現状の実装ルール」を正とする**（docx より新しい）。

## ゴール
2〜4人用、claude.ai Artifact 上で動作する **React JSX シングルファイル** のプレイアブルデモ。
- ファイル名：`hojo_suiden_v2.jsx`（旧 `hojo_suiden.jsx` は別物・参照のみ）
- ※ 複数端末でのオンライン対戦化を検討中（方式未定。現状はローカルのホットシート）

## 旧版→改訂版の骨子（不変の方針）
| 項目 | 改訂版 |
|------|--------|
| 経済 | **米本位制**。俵がそのまま通貨＝勝利点（市場・売却なし） |
| 土地 | **開墾**で増やす。位階で上限管理 |
| 労働力 | **働き手を毎ターン配置**。年度末のみ増員・維持費あり |
| 評判 | **第二の通貨**。質で増え量産で減る |
| 年数 | **6年（8ラウンド×6＝48ラウンド）** |
| 勝利点 | 俵の等級＋評判＋米寿ボーナス |

---

## ゲームの構造

### ゲーム長
6年。1年 = 8ラウンド（春R1-2／夏R3-4／秋R5-6／冬R7-8）。各年R8の後に「年度末フェーズ」。

### 1ラウンドの手順
```
フェーズ0：天候（全員同時）天候カード1枚＋ダイス2個 → 全員に一斉適用
フェーズ1：行動（手番順）  手元の働き手3個を配置
フェーズ2：成長（全員同時）水位で成長判定（洪水は流出）→ 全田 水位−1蒸発（曇りは蒸発なし）
                            成熟したまま放置の田は2R毎に品質−1（並まで）
フェーズ3：精算（全員同時）手番マーカーを次へ → 次ラウンド
```

### 年度末フェーズ（各年R8後）
**処理順（出費を先に済ませてから雇用を判断させる）：**
```
1. 租      ：田の数 × 1俵
2. 保管リスク：保管量に応じて俵減少（倉で半減）— 下表
3. ネズミの大量発生：レア・甚大（後述）
4. 維持費  ：労働者数 × 1俵。払えない分だけ労働者が離脱＋離脱1人につき評判−1
5.〔質問〕雇用：1個 = 4俵（ここでのみ増員可）
6.〔質問〕位階昇進：評判を消費（任意）
```
※ コード内のステップ名は A=租 / B=保管 / D=維持費 / C=雇用 / E=昇進。**実行順は上記（D を C より前に）**。

---

## アクション（フェーズ1）
| アクション | 働き手 | 季節 | 効果 |
|-----------|--------|------|------|
| 植え付け | 1 | 春・夏 | 空き田に品種を植える（苗があれば成長+1開始／耕地なら品質+1開始） |
| 水を引く | 1 | 通年 | 水位+2（**コストなし**） |
| 肥料 | 1 | 通年 | **品質肥料**=品質+1（上限まで）／**成長肥料**=成長+1。支払いは堆肥1 or 俵1 |
| 開墾 | 1 | 通年 | 荒れ地ゲージ+1（道具+1・牛+1で加速） |
| 収穫 | 1〜2 | **通年** | 成熟田から俵。働き手1=通常量／2=豊作量。道具で+1俵 |
| 献上 | 2 | 年1回 | 上質/特上を1俵納め評判+2 |
| 出稼ぎ | **2** | **秋・冬** | 並2俵を得る（農閑期の収入・詰み回避） |
| 育苗 | 1 | 秋・冬 | 苗+1（翌春以降の植え付けで成長+1に使用） |
| 土づくり | 1 | 秋・冬 | 空き田を耕す→その田の次作が品質+1で開始 |
| 藁仕事 | **2** | 秋・冬 | 評判+1（年1回） |
| 堆肥作り | 1 | 秋・冬 | 堆肥+2（肥料の俵コストを肩代わり） |
| 購入 | 0 | 通年 | 道具/牛/倉を購入（俵 or 評判・働き手不要） |
| 行動終了 | — | 通年 | 手番終了 |

---

## 主要システム

### 俵（米本位制）
- 通貨かつ勝利点。等級：並=1点／上質=2点／特上=3点
- 支払いは等級不問（並→上質→特上の順で消費）。最終得点で等級ぶん換算

### 評判（第二の通貨）
- **増**：上質/特上の収穫 +1、献上 +2、藁仕事 +1
- **減**：1回の収穫で並4俵以上 −1、維持費未払いで離脱した労働者1人につき −1
- **使い道**：道具/牛/倉の評判払い、位階昇進（評判専用・逓増）、最終点（評判1=1点）

### 労働力
- 初期3個。毎ターン配置→ターン終了で手元に戻る
- 増員は年度末のみ（1個=4俵）。自発的リストラ不可。維持費1俵/個/年
- **維持費を払えないと、不足分だけ労働者が離脱＋評判−1/人**

### 土地・開墾
- 開墾：働き手1でゲージ+1（道具+1・牛+1）、ゲージ3で田1枚完成
  - 上限内なら即・上限超なら待機し、位階昇進時に自動変換
- **初期土地上限4（位階0）・初期荒れ地4枚**（昇進前でも開墾で増やせる余裕）
- 位階昇進：評判 **3/6/10**（逓増）、各 +2枚（上限 4→6→8→10）、荒れ地+2枚

### 品種
| 品種 | 成長R | 収穫量 | 品質上限 | コスト | 特徴 |
|------|-------|--------|----------|--------|------|
| 野良稲 | 3 | 2 | 並 | 0 | 無料の保険（金欠時） |
| 早稲 | 2 | 3〜4 | 並 | 1 | 速い量産・評判は伸びない |
| 中稲 | 3 | 3〜4 | 上質 | 2 | 平均・追肥で上質 |
| 晩稲 | 4 | 2〜3 | 特上 | 2 | 少量高品質（初期から上質） |
- 品質肥料で品質+1（品質上限まで）／成長肥料で成長+1（田1枚につき各1回）
- 牛があれば植え付けコスト−1
- 成熟後に放置すると2R毎に品質−1（並まで）→ 早めの収穫を促す

### 育苗・土づくり・堆肥（秋冬の仕込み）
- **育苗**：苗をストック。翌春以降、植え付け時に苗1を消費すると成長+1の状態で開始
- **土づくり**：空き田を耕すと、その田に次に植える作物が品質+1で開始（1回分）
- **堆肥**：ストックした堆肥は、肥料を使うとき俵の代わりに自動消費（堆肥優先）

### 水管理（0〜5、最適2〜3）
- 0=干ばつ(成長なし)／1=低水位(成長なし)／2-3=最適(成長+1)／4=病害(成長なし)／5=洪水(作物流出)
- 毎ラウンド−1蒸発（曇りカードの年は蒸発なし）、「水を引く」で+2

### 天候
- ダイス2個：☀️(1-2)水位−1／💧(3-4)水位+1／🌬️(5)風(追加天候)／✨(6)成長+1
- カードデッキ（現行）：
  - 晴れ×3（通常）／曇り（蒸発なし）
  - 台風（**全田 品質−1**）／冷夏（夏:成長−1）／早霜（秋:晩稲 成長−2）／大雪（冬:次の春−1行動）
  - 小春日和×2（並俵+2）／大雨（水位+2）／恵みの露（水位+1）／干ばつ（水位−2）／豊穣の兆し×2（成長+1）

### 保管リスク（年度末・段階表）
| 保管量 | 損失 |
|--------|------|
| 〜10 | なし |
| 11〜20 | −1 |
| 21〜30 | −2 |
| 31+ | −3 |
※倉があれば半減（端数切上）。通常の損失は軽め＝下のネズミ大発生が脅威の主役。

### ネズミの大量発生（レア・甚大）
- 2年目以降、各年 **20%** で発生（**ゲーム1回まで**）
- 発生時、各自 **備蓄の半分が消失**。倉があれば **1/4** に軽減
- → 倉の主目的はこの保険

---

## 数値早見表
| 項目 | 値 |
|------|-----|
| 初期俵 | 8 |
| 初期働き手 | 3 |
| 初期の田 | 2枚 |
| 初期土地上限 | 4（位階0） |
| 初期荒れ地 | 4枚 |
| 租 | 田1枚=1俵/年 |
| 維持費 | 1個=1俵/年（未払いで離脱＋評判減） |
| 雇用費 | 1個=4俵（年度末のみ） |
| 開墾ゲージ | 3で完成 |
| 道具 | **8俵/評判4**（開墾+1・収穫+1） |
| 牛 | **16俵/評判6**（開墾+1・植付−1） |
| 倉 | **6俵/評判3**（保管半減・ネズミ被害1/4） |
| 位階昇進 | 評判3/6/10（各+2枚） |
| 献上 | 年1回・働き手2・評判+2 |
| 出稼ぎ | 秋冬・働き手2・並2俵 |
| 藁仕事 | 秋冬・働き手2・評判+1（年1回） |
| 育苗 | 秋冬・働き手1・苗+1 |
| 土づくり | 秋冬・働き手1・次作品質+1 |
| 堆肥作り | 秋冬・働き手1・堆肥+2 |
| 米寿 | 累計88点で+10点 |
| ゲーム長 | 8R×6年 |

---

## 最終得点
```
俵の等級合計（並1/上質2/特上3）+ 評判×1 + 米寿ボーナス（88点で+10）
```

---

## 実装方針（claude.ai Artifact 制約）
- **localStorage / sessionStorage 使用禁止**。状態は React state（useState/useReducer）のみ
- 外部ライブラリは Tailwind コアクラスのみ（lucide-react も未使用＝アイコンは絵文字）
- `<form>` タグ不可。`onClick`/`onChange` を使う
- シングルファイル、default export のコンポーネント

### アーキテクチャ
- ゲームロジックは**純粋関数**＋`deepClone`（JSON）で不変更新
- `const update = useCallback(fn => setGame(prev => { const g = deepClone(prev); fn(g); return g; }), [])`
- 主要関数：`createGame` / `doDrawWeather` / `doApplyWeather` / `doGrowthPhase` / `executeAction`
  / `beginYearEnd` / `doYearEndMaintenance` / `doYearEndHire` / `doYearEndRankUp` / `finishYearEnd`
  / `doAdvanceTime` / `doEndGame` / `computeGameMetrics` / `takeYearSnapshot`
- 画面：SetupScreen / GameScreen（Header・プレイヤータブ・PlayerBoard・ActionPanel・EventLog）/ ResultScreen

### state 構造（現行）
```js
{
  phase: "weather_draw"|"weather_apply"|"action"|"year_end"|"result",
  year:1-6, seasonIdx:0-3, roundInSeason:0-1,
  turnOrder:[...], actionTurnIdx,
  weatherDraw:{card,dice,effects}, weatherDeck:[...], cloudyThisRound,
  ratOutbreakDone,
  yearEndStep:"C"|"E"|null, yearEndPlayerIdx,
  players:[{
    id, name,
    rice:[{quality:1-3,count}],          // 等級別の俵
    reputation, rank, landLimit,
    workers, workersUsed,
    tools:{plow,ox,barn},
    seedlings, compost,                  // 育苗ストック・堆肥ストック
    donatedThisYear, strawworkThisYear,  // 年1回制限
    penaltyNextSpring,                   // 大雪の翌春ペナルティ
    fields:[{ id, status:"empty"|"planted"|"mature", variety, growth, requiredGrowth,
              quality, water:0-5, fertilized, growthFertilized, overripe, tilled }],
    wildlands:[{ id, gauge:0-3 }],
  }],
  log:[],
  // ── プレイデータ記録（バランス分析用）──
  gameId, playerCount, events:[], yearSnapshots:[], exportData,
}
```

### プレイデータ出力
- `events`（全行動・天候・年度末を構造化記録）、`yearSnapshots`（年度末の全員状態）、`metrics`（集計）を保持
- ResultScreen の「📊 プレイデータ（JSON）」からコピー可。Claude に貼って傾向分析・バランス調整に使う

---

## 実装状況
- 段階ビルド（骨格→天候/水/植付/成長→収穫→労働力→開墾→評判→道具/租/保管→得点→仕上げ）は**完了**
- 以降は友人プレイテストを反映した**バランス調整を反復中**（本書はその最新状態）

## 未確定（プレイテストで詰める数値）
- 道具8俵/牛16俵が「2年目に道具・4年目に牛」の体感に合うか
- 出稼ぎ（秋冬・2人で2俵）・堆肥作り（+2）・藁仕事（2人）の強さ
- ネズミ大発生の確率20%・被害率（半減/倉1/4）、熟成劣化の速さ（2R毎）
- 晩稲の収穫量（2〜3）、保管リスク表の刻み
- 上級ルール「共同年貢」は後付けモジュール（初版デモには入れない）
- オンライン対戦化の方式 → **boardgame.io に決定**（次章「オンライン対戦化」を参照）

---

# オンライン対戦化（boardgame.io 採用）

> 目的：友人がそれぞれの端末から接続して遊ぶ。画面共有では成立しない**隠匿情報**（伏せた行動など）を入れたい。
> 方針：現行の Artifact 単体デモ（`hojo_suiden_v2.jsx`）を土台に、ロジックを boardgame.io の `moves` へ移植する。
> **採用理由**：隠匿情報（`playerView`）とターン/フェーズ管理がフレームワーク標準。今の「純粋関数＋不変更新」の書き方とほぼ一致するため移植が素直。

## なぜ boardgame.io が今の実装と相性が良いか
- 現行ロジックは既に **純粋関数＋deepClone の不変更新**。boardgame.io の move は「G を受け取り変更する関数」で、思想が同じ。
- `executeAction(g, type, params)` の分岐が、そのまま **各 move** に一対一で割り付けられる。
- フェーズ進行（天候→行動→成長→精算、年度末）は boardgame.io の **phases / turn** に対応づけられる。

## アーキテクチャの対応表（現行 → boardgame.io）
| 現行（Artifact） | boardgame.io |
|------------------|--------------|
| `game` state オブジェクト | `G`（ゲーム状態） |
| `turnOrder` / `actionTurnIdx` / 手番 | `ctx`（current player・turn・phase が標準提供） |
| `executeAction` の各 type | `moves`（plant / irrigate / fertilize / reclaim / harvest / donate / migrantWork / raiseSeedling / tillSoil / strawWork / makeCompost / buy …） |
| `doDrawWeather` / `doApplyWeather` | フェーズ `weather` の `onBegin` などで処理（サーバ権威） |
| `doGrowthPhase` | フェーズ遷移の `onEnd` / `endIf` で処理 |
| 年度末 `beginYearEnd`…`finishYearEnd` | `phase: "yearEnd"`（順序 1.租→2.保管→3.ネズミ→4.維持→5.雇用→6.昇進 を維持） |
| `App`/`GameScreen`（state を直接操作） | `Client` に渡す **Board コンポーネント**（`G`/`ctx`/`moves` を props で受ける） |

## 隠匿情報の設計（最重要）
- `playerView: PlayerView.STRIP_SECRETS` を設定する。
- 仕組み：`G.secret` は**全員から**除去、`G.players[playerID]` は**本人以外から**除去された状態で各クライアントへ届く。
- このゲームでの隠匿候補（オンライン化で追加したい戦略要素）：
  - 伏せて出す行動（例：年度末の供出量、同時公開する行動選択）→ `G.players[id].hiddenChoice`
  - 誰も見てはいけない山札順（天候デッキの残り）→ `G.secret.weatherDeck`
- **注意**：秘密情報に触る move は `client: false` を付け、**サーバー側でのみ実行**（クライアントは全データを持たないため）。
  - 例：天候デッキを引く処理、伏せ札の一斉公開は server-authoritative にする。
- 現行で全プレイヤー状態を1つの `players[]` に持っているが、オンラインでは
  **「自分に見えてよい情報」と「隠す情報」を `G.players[id]`／`G.secret` に振り分ける**再設計が要る。

## セットアップ方針（エージェント＝Claude Code に任せる前提）
- **`create-react-app` は使わない（メンテ終了）。`Vite` + React で雛形を作る**。
- `npm i boardgame.io` を追加。クライアントは `boardgame.io/react` の `Client`、通信は `boardgame.io/multiplayer` の `SocketIO`。
- ローカル開発は **フロント（Vite dev server）＋ サーバ（`boardgame.io/server`）の2プロセス**を起動して接続確認。
- まず**1台で2タブ**を別 playerID で開き「隠匿を保ったまま1手」を確認 → その後に別端末接続。

## 公開（友人が遊ぶ）
- マルチプレイはサーバが要るため、**フロント（Vercel/Netlify 等）＋ Node サーバ（Render 等）**の2段構え。
- いずれも無料枠で個人プレイテスト規模は足りる見込み。
- 接続の入口は `Lobby`（boardgame.io 同梱）を使うと、友人が部屋に参加しやすい。

## 工程ごとの担当
| 工程 | Claude Code | 自分（人間） |
|------|-------------|--------------|
| Vite + boardgame.io 雛形 | ◎ ほぼ全部 | 確認のみ |
| `executeAction`→`moves` 移植 | ◎ ほぼ全部 | レビュー |
| フェーズ/年度末を phases へ | ◎ 書ける | 順序の確認 |
| 隠匿の振り分け（players/secret） | ◎ 書ける | 「どの手が秘密に触るか」を指示 |
| ローカル2タブ動作確認 | ○ 起動まで | 実プレイ確認 |
| デプロイ（Vercel/Render） | △ 設定・コード | ◎ アカウント・公開操作 |

## 移植の進め方（推奨順）
1. **Artifact 版を「正」として固める**（バランス調整が一段落するまではこちらで反復）。
2. boardgame.io 雛形を作り、**隠匿なしのホットシート相当**をまず動かす（移植の正しさ確認）。
3. `playerView` と `G.secret`/`G.players` を導入し、**隠匿情報を1つ**入れて2タブで検証。
4. 別端末接続 → デプロイ → 友人とプレイテスト。
5. オンライン特有の体験（伏せ行動の読み合い等）を、Artifact 版のルールへ逆輸入するか検討。

## 留意点
- boardgame.io は公式 npm が長く更新停止（0.50系）。**Vite との組み合わせや最新 React での型・ESM 周りで詰まる可能性**があるため、雛形作成時にエラーが出たら設定（vite config / ESM）を優先的に疑う。
- Artifact 制約（localStorage 禁止・単一ファイル・Tailwind のみ）は **Artifact 版だけのルール**。
  boardgame.io 版は通常の React プロジェクトなので、**複数ファイル・任意ライブラリ・ルーティング可**（この制約から解放される）。

---

# オンライン版 実装メモ（`online/` ディレクトリ）

> Artifact 版（`hojo_suiden_v2.jsx`）はリポジトリ直下に温存。boardgame.io 版は `online/` に新規作成する。

## 現在のマイルストーン
- **M1（隠匿なし・ホットシート相当を動かす）に着手**。`online/` に Vite + boardgame.io 雛形＋ロジック移植を作成済み。
- Node 未導入環境のため未実行。人間が `online/` で `npm install` → `npm run server`（別窓で）→ `npm run dev` で起動し、最初の実行エラーから一緒に修正していく。

## M1 のフェーズ設計（単一フェーズ＋G.stageで年度末を表現）
- boardgame.io の phases 機構は使わず、`G.stage: "action"|"yearEnd"` で分岐（フレームワーク起因の不具合を避ける）。
- `turn.onBegin`：ラウンド先頭（playOrderPos===0）かつ action → 天候を引いて適用。手番プレイヤーの workersUsed をリセット（＋春の大雪ペナルティ）。
- 行動 move（plant 等）→ プレイヤーが「行動終了」(`events.endTurn()`)。
- `turn.onEnd`：action で最終プレイヤー → 成長＋ラウンド進行（R8 後は年度末の自動処理＝租/保管/ネズミ/維持を実行し `G.stage="yearEnd"`）。yearEnd で最終プレイヤー → `finishYearEnd`（年++・stage を action へ／6年後は `G.gameOver`）。
- 年度末の対話（雇用・昇進）は `yearEndDecision(hireCount, doRankUp)` move を各プレイヤーが1回。
- `endIf`：`G.gameOver` なら最終得点を返す。

## M2 以降
- `playerView: PlayerView.STRIP_SECRETS` と `G.secret.weatherDeck` を導入（天候デッキを隠す）。
- 伏せ行動など隠匿要素を1つ追加して2タブ検証 → 別端末 → デプロイ。
